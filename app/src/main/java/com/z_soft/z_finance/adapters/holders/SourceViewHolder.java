package com.z_soft.z_finance.adapters.holders;

import android.view.View;

public class SourceViewHolder extends TreeViewHolder {

    // если нужно будет описать специфичные компоненты - это можно будет сделат в этом классе
    // пока же компоненты SourceViewHolder совпадают с TreeViewHolder

    public SourceViewHolder(View view) {
        super(view);
    }

}